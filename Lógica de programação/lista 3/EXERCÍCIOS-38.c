/*O cálculo do valor de uma integral definida, usando o método da aproximação por trapézios, é feito
dividindo o intervalo de integração em n partes iguais e aproximando a função, em cada subintervalo obtido,
por um segmento de reta. O valor da integral é calculado, então, como a soma das áreas dos diversos
trapézios formados.*/

#include <stdio.h>
#include <locale.h>

int main(){	
	setlocale(LC_ALL,"PORTUGUESE");
	
	
	printf();
	scanf();
	
	
	
	return 0;
}